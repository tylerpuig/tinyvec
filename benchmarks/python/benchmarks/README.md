### Benchmarks for Python Client

#### Setup

```bash
# Install dependencies
poetry install
```

#### Insert Data

```bash
# Insert data
poetry run python insert.py
```

#### Run Benchmarks

```bash
# Run benchmarks
poetry run python run_benchmarks.py
```

#### Results

Results are saved to `benchmarks/python/benchmarks/metrics.json`
